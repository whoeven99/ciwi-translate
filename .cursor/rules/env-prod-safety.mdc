---
description: 查库/运维默认测环境；查线上需明示；改产数据须二次确认
alwaysApply: true
---

# 测 / 产环境交互约定（Agent ↔ 用户）

本规则约束 **Cursor Agent 与用户的交互行为**，不要求改脚本默认值。

## 查询（读）

1. **默认一律测试环境**（`.env.test` / `.env.worker.test` / Spark `.env.test` 等）。
2. **只有**用户明确说「查线上 / 查生产 / 查 prod / 线上环境」时，才使用生产凭据（`--env=.env.prod` 等）。
3. 用户未点明环境时，**禁止**自行切到 prod 查 Turso / Redis / Cosmos / Render 产服务数据。

## 脚本口径（与交互一致）

- 无参数 → 测环境栈（`.env.test` → `.env.worker.test` → `.env`）。
- 查产 → 显式 `--env=.env.prod`（只读即可）。
- **改产数据** → 必须 `--env=.env.prod` **且** `--confirm-prod`（由 `assertProdWriteAllowed` 硬拦）；Cursor Agent 仍须与用户二次确认后再执行。

## 写入（改数据）

1. 测试环境：可按任务需要写入；破坏性操作仍应说明影响范围。
2. **生产环境任何写操作**（含 `--write` / `--apply` / resume 任务 / migrate prod / 删 Redis·Cosmos·Turso 数据 / 改 hint 队列等）：
   - **必须与用户二次确认**（第一次说明要做什么与风险；第二次得到明确「确认执行 / 可以改产」后再动手）。
   - 仅说「查一下线上」**不等于**授权改产数据。
   - 脚本侧另需 `--confirm-prod`，缺则拒绝执行。

## 回复时

- 执行查询前可一句话标明当前目标环境：`测` 或 `产`。
- 若用户意图含糊（例如「看看那个任务」），默认按 **测**，并在需要时问是否要改查产。
