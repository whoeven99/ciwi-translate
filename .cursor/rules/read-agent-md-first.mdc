---
description: 每次改代码前必须先阅读 AGENTS.md 再定位相关文件
alwaysApply: true
---

# 改代码前先读 AGENTS.md

在本仓库中，**动手改任何文件之前**（包括编辑、新增、删除），都必须按以下顺序执行：

1. **先阅读** 仓库根目录的 `AGENTS.md`，根据任务识别对应功能区、路由、server helper、worker、extension、script 或 Prisma model。
2. **先读协作 skill**：完整读取并遵循 `.cursor/skills/deliberate-collab/SKILL.md`（先确认选型 → P0/P1 方案/UI 样例 → 默认只做 P0）。
3. **再** 按 `AGENTS.md` 中的路径与章节定位具体源码；不要跳过索引直接全库盲搜。
4. **改之前** 运行 `git status --short`，避免覆盖用户改动或无关未跟踪文件。
5. 若 `AGENTS.md` 已指向明确入口文件，优先从那些文件读起，再按调用链向下。
6. 改动应落在该功能边界内；若发现索引过时或缺失，在完成任务后同步更新 `AGENTS.md` 对应章节。

未读 `AGENTS.md` 与 deliberate-collab skill 不得开始编辑。`AGENTS.md` 的「Required Workflow」与各功能章节优先于临时猜测。
